#include <bits/stdc++.h>
using namespace std;

void printsbsqnce(string input,string output){
    if(input.empty()){
        cout << output << endl;
        return;
    }

    printsbsqnce(input.substr(1), output+input[0]);
    printsbsqnce(input.substr(1), output);
}

int main(){
  string input;
  cin >> input;
  string output = "";
 printsbsqnce(input, output);
 return 0;
}